package net.romeosnowblitz.hmh2.screen.slot;

/*
public class ModCoolantSlot extends Slot {
    public ModCoolantSlot(Inventory inventory, int index, int x, int y) {
        super(inventory, index, x, y);
    }

    @Override
    public boolean canInsert(ItemStack stack) {
        return ModAbstractFurnaceBlockEntity.canUseAsFuel(stack) || ModCoolantSlot.isBucket(stack);
    }

    @Override
    public int getMaxItemCount(ItemStack stack) {
        return ModCoolantSlot.isBucket(stack) ? 1 : super.getMaxItemCount(stack);
    }

    public static boolean isBucket(ItemStack stack) {
        return stack.isOf(Items.BUCKET);
    }
}

 */
